Name: Osher Shuman
Student Number: SHMOSH001
Date Due: 28 April 2015

Description:
	driver.cpp - Runs at start of program. Checks option chosen to manipulate image and calls the appropriate method.
	image.cpp - Image class that allows the manipulation of pgm images. The method available are add, subtract, inverse, mask and threshold.
		    The class also has a load and save method. The load method reads a pgm file into an Image object and the save method creates a pgm file from an image object.

Instructions: 
1. Type "make" into terminal.
2. type "imageops <option> OutputImageName" where option could be -a, -s, -i, -l, -t
3. When done type "make clean" into console.
  